
ToDo
+ remove values from config.php
+ replace internal autoloader with psr
+ remove "if (!function_exists('ipFile')) {" from functions.php
+ remove $ipFile_overrides
+ handle "file" dir
- 'TTF_folder' => ipFile('Ip/Lib/HnCaptcha/fonts/'),
+ remove config.php from GIT to ease the development
- update readme

Incompatabilities:

- removed support of fileOverrides

Test:

- theme / plugin view overrides
- windows install / update / overrides
- repository
- css compile
- core compile
-
